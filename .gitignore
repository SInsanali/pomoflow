# Personal focus history exported from v1 — never publish
v1-data-export.json

# Icon artwork drops — the four PNGs in src/icons are what ships
*.zip

# Python (only tools/make-icons.py remains, superseded; the v1 server is gone)
__pycache__/
*.py[cod]
.venv/
venv/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude Code — local settings and scratch worktrees, per-machine by nature
.claude/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
